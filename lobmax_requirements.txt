# LOBMAX Conda Environment Requirements
# --------------------------------------
# Create environment: conda create -n lobmax python=3.12 -y
# Activate: conda activate lobmax
# Install: pip install -r lobmax_requirements.txt

# Core JAX/Flax
jax>=0.8.0
jaxlib>=0.8.0
flax>=0.12.0
optax>=0.2.0
orbax-checkpoint>=0.11.0

# PyTorch (for dataloading)
torch>=2.0.0
torchaudio>=2.0.0
torchvision>=0.15.0

# TensorFlow (required by MaxText)
tensorflow>=2.15.0
tensorflow-text>=2.15.0

# MaxText dependencies
omegaconf>=2.3.0
ml-collections>=1.0.0
grain>=0.2.0
tensorboardX>=2.6.0

# Data processing
numpy
pandas
seaborn
matplotlib

# HuggingFace
datasets
huggingface_hub

# GCP (required by MaxText)
google-cloud-storage
gcsfs

# Utilities
wandb
tqdm
einops
pyyaml
python-dateutil

# Note: After installing, you may also need:
# pip install -e /path/to/maxtext[cuda12]
